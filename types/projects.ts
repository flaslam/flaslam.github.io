export interface Project {
  name: string;
  description?: string;
  languages?: string[];
  technologies?: string[];
  link?: string;
  source?: string;
  features?: string[];
  image?: string;
  internal?: boolean;
}
